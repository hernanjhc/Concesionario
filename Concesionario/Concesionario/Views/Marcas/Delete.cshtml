@model Concesionario.Models.Marcas

@*@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h1>¿ Está seguro que desea eliminar @Model.Marca ?</h1>

@using (Html.BeginForm("Eliminar", "Marcas", new { id = Model.Id }, FormMethod.Post))
{
    <td colspan="2"><button type="submit" class="btn btn-primary">Eliminar</button></td>
}*@

<div class="modal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Modal title</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                @{
                    ViewBag.Title = "Delete";
                }

                <h2>Delete</h2>

                <h1>¿ Está seguro que desea eliminar @Model.Marca ?</h1>

                @using (Html.BeginForm("Eliminar", "Marcas", new { id = Model.Id }, FormMethod.Post))
                {
                    <td colspan="2"><button type="submit" class="btn btn-primary">Eliminar</button></td>
                }
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary">Save changes</button>
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

